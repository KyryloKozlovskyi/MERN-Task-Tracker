import React from "react";

// CreateTask component
const CreateTask = () => {
  // Returns the relevant message
  return (
    <div>
      <h3>Hello from the CreateTask component</h3>
    </div>
  );
};

export default CreateTask; // Exports the component
